"use client";
import Link from"next/link";import{usePathname}from"next/navigation";import{CalendarDays,Headphones,Home,Menu,Sparkles,X}from"lucide-react";import{useState}from"react";import{ImmersiveEffects}from"./immersive-effects";import{UniverseSwitch}from"./universe-switch";
const nav=[["Inicio","/"],["Fechas","/fechas"],["Música","/musica"],["Lanzamientos","/lanzamientos"],["Media","/media"],["Historia","/historia"],["Comunidad","/comunidad"],["EPK","/epk"],["Booking","/booking"]];
export function SiteShell({children}:{children:React.ReactNode}){const[open,setOpen]=useState(false);const pathname=usePathname();if(pathname.startsWith("/admin"))return <>{children}</>;const active=(href:string)=>href==="/"?pathname===href:pathname.startsWith(href);const close=()=>setOpen(false);return <><ImmersiveEffects/><a className="skip-link" href="#contenido">Saltar al contenido</a><header className="topbar"><Link className="wordmark" href="/" aria-label="IAMJOSHWA, inicio">IAMJOSHWA<span>®</span></Link><nav className="desktop-nav" aria-label="Principal">{nav.map(([label,href])=><Link key={href} href={href} aria-current={active(href)?"page":undefined}>{label}</Link>)}</nav><UniverseSwitch/><button className="menu-button" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="mobile-menu" aria-label={open?"Cerrar menú":"Abrir menú"}>{open?<X/>:<Menu/>}</button></header>{open&&<nav id="mobile-menu" className="mobile-menu" aria-label="Menú móvil">{nav.map(([label,href])=><Link key={href} href={href} onClick={close} aria-current={active(href)?"page":undefined}>{label}</Link>)}</nav>}<main id="contenido">{children}</main><footer><div><span className="wordmark">IAMJOSHWA</span><p>DJ & Producer — Ciudad de México</p></div><p>© {new Date().getFullYear()} IAMJOSHWA. Sitio oficial.</p></footer><nav className="bottom-nav" aria-label="Accesos rápidos"><Link href="/" aria-current={active("/")?"page":undefined}><Home/><span>Inicio</span></Link><Link href="/fechas" aria-current={active("/fechas")?"page":undefined}><CalendarDays/><span>Fechas</span></Link><Link href="/musica" aria-current={active("/musica")?"page":undefined}><Headphones/><span>Escuchar</span></Link><Link href="/acceso" aria-current={active("/acceso")?"page":undefined}><Sparkles/><span>Pass</span></Link><button onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="mobile-menu"><Menu/><span>Menú</span></button></nav></>}
