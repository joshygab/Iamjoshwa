"use client";
import{useState}from"react";const types=["Club","Festival","Rave","Evento privado","Evento corporativo","Evento universitario","Showcase","Opening","Closing"];
export function BookingForm({enabled}:{enabled:boolean}){const[state,setState]=useState<{loading:boolean;error?:string;folio?:string}>({loading:false});async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();if(!event.currentTarget.checkValidity()){event.currentTarget.reportValidity();return}if(!enabled){setState({loading:false,error:"El envío todavía no está conectado. La información permanece en este formulario y no se ha guardado ni enviado."});return}setState({loading:true});const element=event.currentTarget;const form=new FormData(element);const body={name:form.get("name"),company:form.get("company"),email:form.get("email"),whatsapp:form.get("whatsapp")||undefined,eventType:form.get("eventType"),eventDate:form.get("eventDate"),eventTime:form.get("eventTime")||undefined,city:form.get("city"),venue:form.get("venue")||undefined,attendance:form.get("attendance")||undefined,setDuration:form.get("setDuration")||undefined,project:form.get("project"),genres:String(form.get("genres")||"").split(",").map(x=>x.trim()).filter(Boolean),budget:form.get("budget")||undefined,equipment:form.get("equipment")||undefined,production:form.get("production")||undefined,message:form.get("message"),consent:form.get("consent")==="on",website:form.get("website")};try{const response=await fetch("/api/booking",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const data=await response.json();if(!response.ok)throw new Error(data.error);setState({loading:false,folio:data.folio});element.reset()}catch(error){setState({loading:false,error:error instanceof Error?error.message:"No fue posible enviar."})}}
if(state.folio)return <div className="success-state compact"><span>✓</span><h2>Solicitud recibida.</h2><p>Tu folio es <strong>{state.folio}</strong>. Guárdalo para seguimiento.</p><button className="button secondary" onClick={()=>setState({loading:false})}>Nueva solicitud</button></div>;return <form className="booking-form" onSubmit={submit}><input className="honeypot" name="website" tabIndex={-1} autoComplete="off" aria-hidden="true"/><div className="field-grid"><label>Nombre *<input name="name" minLength={2} maxLength={100} required autoComplete="name"/></label><label>Empresa / promotor *<input name="company" minLength={2} maxLength={120} required autoComplete="organization"/></label><label>Correo *<input name="email" type="email" required autoComplete="email"/></label><label>WhatsApp<input name="whatsapp" type="tel" autoComplete="tel"/></label><label>Tipo de evento *<select name="eventType" required defaultValue=""><option value="" disabled>Selecciona</option>{types.map(x=><option key={x}>{x}</option>)}</select></label><label>Fecha *<input name="eventDate" type="date" required/></label><label>Horario<input name="eventTime" type="time"/></label><label>Ciudad *<input name="city" minLength={2} required autoComplete="address-level2"/></label><label>Venue<input name="venue"/></label><label>Asistentes<input name="attendance" type="number" min="1" max="1000000"/></label><label>Duración<select name="setDuration" defaultValue="90"><option value="60">60 min</option><option value="90">90 min</option><option value="120">120 min</option></select></label><label>Proyecto *<select name="project"><option value="iamjoshwa">IAMJOSHWA</option><option value="afterluv">AFTERLUV</option></select></label></div><label>Géneros<input name="genres" placeholder="House, Tech House"/></label><label>Presupuesto aproximado<input name="budget" placeholder="Rango y moneda"/></label><label>Equipo disponible<textarea name="equipment" rows={2}/></label><label>Audio e iluminación<textarea name="production" rows={3}/></label><label>Mensaje *<textarea name="message" rows={5} minLength={20} maxLength={5000} required aria-describedby="message-help"/><small id="message-help">Incluye formato, horarios y cualquier requerimiento importante.</small></label><label className="checkbox"><input name="consent" type="checkbox" required/>Acepto ser contactado sobre esta solicitud. *</label>{state.error&&<div className="error-alert" role="alert">{state.error}</div>}<button className="button primary" disabled={state.loading}>{state.loading?"Enviando…":enabled?"Enviar solicitud":"Validar solicitud"}</button>{!enabled&&<p className="form-note">Modo demostración: el formulario valida los datos, pero no los envía.</p>}</form>}
