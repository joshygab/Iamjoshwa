"use client";
import Image from"next/image";import{createClient}from"@/lib/supabase/client";import{isSupabaseConfigured}from"@/lib/env";import{useUniverse}from"./universe-provider";import{Countdown}from"./countdown";import type{ReleaseItem}from"@/types/content";
export function ReleaseLibrary({items}:{items:ReleaseItem[]}){const{universe}=useUniverse();async function presave(item:ReleaseItem){if(!isSupabaseConfigured||item.demo)return;const db=createClient();const{data:{user}}=await db.auth.getUser();if(!user)return;await db.rpc("record_fan_action",{p_action:"presave_click",p_source_id:item.id})}const visible=items.filter(item=>item.universe===universe);if(!visible.length)return <div className="admin-empty"><h2>Sin lanzamientos publicados.</h2><p>Vuelve pronto para escuchar la siguiente señal.</p></div>;return <div className="release-grid">{visible.map(item=>{const future=new Date(item.releaseAt)>new Date();const href=future?item.presaveUrl:item.listenUrl;return <article className="release-card" key={item.id}><div className="release-cover">{item.coverUrl&&<Image src={item.coverUrl} alt={`Portada de ${item.title}`} fill sizes="(max-width:760px) 100vw, 42vw"/>}<span className="demo-badge">{item.demo?"LANZAMIENTO DEMO":future?"PRÓXIMAMENTE":"DISPONIBLE"}</span></div><div><span>{item.type}</span><h2>{item.title}</h2><p>{item.story}</p>{future&&<Countdown date={item.releaseAt}/>} {item.credits?.length&&<p className="muted">{item.credits.join(" · ")}</p>}<div className="inline-actions">{href?<a className="button primary" href={href} onClick={()=>future&&void presave(item)} target="_blank" rel="noreferrer">{future?"Haz pre-save":"Escuchar ahora"}</a>:<button className="button primary" disabled>{future?"Pre-save pendiente":"Enlaces pendientes"}</button>}{item.platforms?.map(link=><a className="button secondary" key={link.label} href={link.url} target="_blank" rel="noreferrer">{link.label}</a>)}</div></div></article>})}</div>}
