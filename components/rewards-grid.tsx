"use client";
import {useState}from"react";import {createClient}from"@/lib/supabase/client";import type{RewardItem}from"@/types/content";import {useUniverse}from"./universe-provider";
export function RewardsGrid({items,balance}:{items:RewardItem[];balance:number}){const{universe}=useUniverse();const[points,setPoints]=useState(balance);const[state,setState]=useState<{ok?:boolean;message:string}|null>(null);const visible=items.filter(x=>!x.project||x.project===universe);async function redeem(id:string){setState({ok:true,message:"Procesando..."});const db=createClient();const{data,error}=await db.rpc("redeem_reward",{p_reward_id:id});if(error){setState({ok:false,message:error.message});return}const payload=data as {remaining_points?:number};if(typeof payload.remaining_points==="number")setPoints(payload.remaining_points);setState({ok:true,message:"Recompensa canjeada. Revisa tu perfil."})}return <><p className="section-kicker">SALDO · {points} PUNTOS</p>{state&&<p className={state.ok?"success-alert":"error-alert"} role="status">{state.message}</p>}<div className="list-grid">{visible.map(item=><article className="list-card" key={item.id}><span className="demo-chip">{item.inventory===null?"DIGITAL":`${item.inventory} DISPONIBLES`}</span><h2>{item.name}</h2><p>{item.description}</p><strong>{item.pointsCost} puntos</strong><button className="button primary" disabled={points<item.pointsCost||item.inventory===0} onClick={()=>redeem(item.id)}>Canjear</button></article>)}</div>{!visible.length&&<div className="admin-empty"><h2>Sin recompensas publicadas.</h2><p>El administrador puede preparar el primer drop desde el CMS.</p></div>}</>}
