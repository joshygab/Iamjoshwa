"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
type EditorValues=Record<string,unknown>&{id?:string};
type Asset={id:string;display_name:string;mime_type:string};
type Props={module:string;initial?:EditorValues;assets?:Asset[]};
export function ContentEditor({module,initial={},assets=[]}:Props){const router=useRouter();const[state,setState]=useState<{loading?:boolean;error?:string;fields?:Record<string,string[]>}>({});async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setState({loading:true});const values=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/admin/content",{method:initial.id?"PATCH":"POST",headers:{"content-type":"application/json"},body:JSON.stringify({module,id:initial.id,values})});const data=await response.json();if(!response.ok)return setState({error:data.error||"No fue posible guardar.",fields:data.fields});router.push(`/admin/${module}`);router.refresh()}async function archive(){if(!initial.id||!confirm("¿Archivar este contenido?"))return;const response=await fetch("/api/admin/content",{method:"DELETE",headers:{"content-type":"application/json"},body:JSON.stringify({module,id:initial.id})});if(response.ok){router.push(`/admin/${module}`);router.refresh()}else setState({error:(await response.json()).error})}const common=<><label>Proyecto<select name="project" defaultValue={text(initial.project,"iamjoshwa")}><option value="iamjoshwa">IAMJOSHWA</option><option value="afterluv">AFTERLUV</option></select></label><label>Estado<select name="publication_status" defaultValue={text(initial.publication_status,"draft")}><option value="draft">Borrador</option><option value="scheduled">Programado</option><option value="published">Publicado</option><option value="archived">Archivado</option></select></label><label>Programar publicación<input name="publish_at" type="datetime-local" defaultValue={dateTime(initial.publish_at)}/></label></>;return <form className="content-editor" onSubmit={submit}>{module==="eventos"&&<>{common}<Text name="name" label="Nombre" value={initial.name} required/><Text name="slug" label="Slug" value={initial.slug} required pattern="[a-z0-9-]+"/><MediaSelect name="flyer_asset_id" label="Flyer" value={initial.flyer_asset_id} assets={assets}/><DateTime name="starts_at" label="Inicio" value={initial.starts_at} required/><DateTime name="doors_at" label="Apertura" value={initial.doors_at}/><DateTime name="set_starts_at" label="Horario del set" value={initial.set_starts_at}/><Text name="venue" label="Venue" value={initial.venue}/><Text name="address" label="Dirección" value={initial.address}/><Text name="city" label="Ciudad" value={initial.city} required/><Text name="country" label="País" value={initial.country||"México"} required/><NumberField name="latitude" label="Latitud" value={initial.latitude} min="-90" max="90"/><NumberField name="longitude" label="Longitud" value={initial.longitude} min="-180" max="180"/><Text name="lineup" label="Lineup, separado por comas" value={array(initial.lineup)}/><Text name="genres" label="Géneros, separados por comas" value={array(initial.genres)}/><Text name="age_restriction" label="Restricción de edad" value={initial.age_restriction}/><NumberField name="price_amount" label="Precio" value={initial.price_amount} min="0"/><Text name="currency" label="Moneda ISO" value={initial.currency||"MXN"}/><Text name="ticket_url" label="Enlace de boletos" value={initial.ticket_url} type="url"/><Text name="promo_code" label="Código promocional" value={initial.promo_code}/><label>Estado del evento<select name="event_status" defaultValue={text(initial.event_status,"upcoming")}>{[["upcoming","Próximamente"],["registration_open","Registro abierto"],["presale","Preventa"],["last_tickets","Últimos boletos"],["sold_out","Sold out"],["waitlist","Lista de espera"],["cancelled","Cancelado"],["rescheduled","Reprogramado"],["completed","Finalizado"]].map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label className="checkbox"><input name="featured" type="checkbox" defaultChecked={Boolean(initial.featured)}/> Destacado</label><Area name="description" label="Descripción" value={initial.description}/><Area name="faq" label="Preguntas frecuentes (JSON)" value={json(initial.faq,[])}/></>}{module==="lanzamientos"&&<>{common}<Text name="name" label="Nombre" value={initial.name} required/><Text name="slug" label="Slug" value={initial.slug} required pattern="[a-z0-9-]+"/><MediaSelect name="cover_asset_id" label="Portada" value={initial.cover_asset_id} assets={assets}/><MediaSelect name="preview_asset_id" label="Fragmento de audio" value={initial.preview_asset_id} assets={assets} kind="audio"/><Text name="release_type" label="Tipo" value={initial.release_type||"Single"} required/><DateTime name="releases_at" label="Estreno" value={initial.releases_at} required/><Text name="presave_url" label="Pre-save" value={initial.presave_url} type="url"/><label className="checkbox"><input name="featured" type="checkbox" defaultChecked={Boolean(initial.featured)}/> Destacado</label><Area name="story" label="Historia" value={initial.story}/><Area name="credits" label="Créditos (JSON)" value={json(initial.credits,[])}/><Area name="release_links" label="Plataformas (JSON)" value={json(initial.release_links,[])}/></>}{module==="sets"&&<>{common}<Text name="title" label="Título" value={initial.title} required/><Text name="slug" label="Slug" value={initial.slug} required pattern="[a-z0-9-]+"/><MediaSelect name="cover_asset_id" label="Portada" value={initial.cover_asset_id} assets={assets}/><Text name="category" label="Categoría" value={initial.category||"Club"}/><Text name="recorded_at" label="Fecha de grabación" value={initial.recorded_at} type="date"/><Text name="location" label="Lugar" value={initial.location}/><NumberField name="duration_seconds" label="Duración en segundos" value={initial.duration_seconds} min="1"/><Text name="genres" label="Géneros, separados por comas" value={array(initial.genres)}/><NumberField name="bpm_min" label="BPM mínimo" value={initial.bpm_min} min="40" max="250"/><NumberField name="bpm_max" label="BPM máximo" value={initial.bpm_max} min="40" max="250"/><NumberField name="energy" label="Energía 1–5" value={initial.energy} min="1" max="5"/><Text name="soundcloud_url" label="SoundCloud" value={initial.soundcloud_url} type="url"/><Text name="youtube_url" label="YouTube" value={initial.youtube_url} type="url"/><Text name="mixcloud_url" label="Mixcloud" value={initial.mixcloud_url} type="url"/><Text name="external_url" label="Enlace externo" value={initial.external_url} type="url"/><label>Acceso<select name="access_level" defaultValue={text(initial.access_level,"public")}><option value="public">Público</option><option value="exclusive">Exclusivo</option></select></label><label className="checkbox"><input name="featured" type="checkbox" defaultChecked={Boolean(initial.featured)}/> Destacado</label><Area name="description" label="Descripción" value={initial.description}/><Area name="set_tracks" label="Tracklist (JSON)" value={json(initial.set_tracks,[])}/></>}{module==="historia"&&<>{common}<Text name="title" label="Título del hito" value={initial.title} required/><Text name="occurred_at" label="Fecha" value={initial.occurred_at} type="date"/><MediaSelect name="asset_id" label="Fotografía" value={initial.asset_id} assets={assets}/><NumberField name="position" label="Posición" value={initial.position||0} min="0"/><Area name="body" label="Historia" value={initial.body}/></>}{module==="epk"&&<>{common}<Text name="section_key" label="Identificador de sección" value={initial.section_key} required pattern="[a-z0-9_]+"/><NumberField name="position" label="Posición" value={initial.position||0} min="0"/><Area name="content" label="Contenido estructurado (JSON)" value={json(initial.content,{})}/></>}{module==="recompensas"&&<>{common}<Text name="name" label="Nombre" value={initial.name} required/><Text name="slug" label="Slug" value={initial.slug} required pattern="[a-z0-9-]+"/><NumberField name="points_cost" label="Costo en puntos" value={initial.points_cost||0} min="0" required/><NumberField name="inventory" label="Inventario" value={initial.inventory} min="0"/><DateTime name="expires_at" label="Fecha límite" value={initial.expires_at}/><Area name="description" label="Descripción" value={initial.description}/><Area name="requirements" label="Requisitos en JSON" value={json(initial.requirements,{})}/></>}{module==="campanas"&&<><Text name="name" label="Nombre" value={initial.name} required/><label>Canal<select name="channel" defaultValue={text(initial.channel,"email")}><option value="email">Email</option><option value="whatsapp" disabled>WhatsApp — requiere proveedor</option><option value="push" disabled>Push — futuro</option></select></label><Text name="trigger_type" label="Disparador" value={initial.trigger_type}/><Text name="template_key" label="Plantilla" value={initial.template_key} required/><Text name="subject" label="Asunto" value={initial.subject}/><Area name="audience_filters" label="Filtros de audiencia en JSON" value={json(initial.audience_filters,{})}/><Area name="template_data" label="Datos de plantilla en JSON" value={json(initial.template_data,{})}/><label>Estado<select name="status" defaultValue={text(initial.status,"draft")}><option value="draft">Borrador</option><option value="scheduled">Programada</option><option value="cancelled">Cancelada</option></select></label><DateTime name="scheduled_at" label="Programar" value={initial.scheduled_at}/></>}{state.error&&<div className="error-alert" role="alert">{state.error}{state.fields&&<ul>{Object.entries(state.fields).map(([key,messages])=><li key={key}>{key}: {messages.join(", ")}</li>)}</ul>}</div>}<div className="editor-actions"><button className="button primary" disabled={state.loading}>{state.loading?"Guardando…":"Guardar"}</button>{initial.id&&<button type="button" className="button danger-button" onClick={archive}>Archivar</button>}</div></form>}
function Text({name,label,value,type="text",required,pattern}:{name:string;label:string;value:unknown;type?:string;required?:boolean;pattern?:string}){return <label>{label}<input name={name} type={type} defaultValue={text(value)} required={required} pattern={pattern}/></label>}
function NumberField({name,label,value,min,max,required}:{name:string;label:string;value:unknown;min?:string;max?:string;required?:boolean}){return <label>{label}<input name={name} type="number" defaultValue={text(value)} min={min} max={max} required={required}/></label>}
function DateTime({name,label,value,required}:{name:string;label:string;value:unknown;required?:boolean}){return <label>{label}<input name={name} type="datetime-local" defaultValue={dateTime(value)} required={required}/></label>}
function Area({name,label,value}:{name:string;label:string;value:unknown}){return <label className="wide-field">{label}<textarea name={name} rows={5} defaultValue={text(value)}/></label>}
function MediaSelect({name,label,value,assets,kind="image"}:{name:string;label:string;value:unknown;assets:Asset[];kind?:"image"|"audio"}){const options=assets.filter(asset=>asset.mime_type.startsWith(`${kind}/`));return <label>{label}<select name={name} defaultValue={text(value)}><option value="">Sin asignar</option>{options.map(asset=><option value={asset.id} key={asset.id}>{asset.display_name}</option>)}</select></label>}
function text(value:unknown,fallback=""){return value==null?fallback:String(value)}function dateTime(value:unknown){if(!value)return"";const date=new Date(String(value));return Number.isNaN(date.getTime())?"":date.toISOString().slice(0,16)}function array(value:unknown){return Array.isArray(value)?value.join(", "):text(value)}function json(value:unknown,fallback:unknown){if(value==null)return JSON.stringify(fallback,null,2);return typeof value==="string"?value:JSON.stringify(value,null,2)}
