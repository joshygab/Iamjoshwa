import{ReleaseLibrary}from"@/components/release-library";import{PageHero}from"@/components/page-hero";import{contentRepository}from"@/lib/data";export const metadata={title:"Lanzamientos"};export default async function ReleasesPage(){const items=await contentRepository.getReleases();return <><PageHero kicker="RELEASES" title="Lo próximo ya está vibrando." description="Pre-save antes del estreno y plataformas oficiales después de la fecha configurada."/><section className="section"><ReleaseLibrary items={items}/><div className="vault-callout"><span>FASE FUTURA</span><h2>THE VAULT</h2><p>Demos, edits, mashups y descargas limitadas. El acceso exclusivo no está activo en esta fase.</p></div></section></>}
