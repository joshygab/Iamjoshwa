import{redirect}from"next/navigation";import{PageHero}from"@/components/page-hero";import{RewardsGrid}from"@/components/rewards-grid";import{contentRepository}from"@/lib/data";import{createClient}from"@/lib/supabase/server";
export const metadata={title:"Recompensas"};export default async function RewardsPage(){const db=await createClient();if(!db)redirect("/acceso");const{data:{user}}=await db.auth.getUser();if(!user)redirect("/acceso");const[{data:points},items]=await Promise.all([db.from("fan_point_totals").select("points").eq("user_id",user.id).maybeSingle(),contentRepository.getRewards()]);return <><PageHero kicker="IAMJOSHWA PASS" title="Convierte la señal en acceso." description="Drops, música y experiencias desbloqueadas con puntos verificados."/><section className="section"><RewardsGrid items={items} balance={points?.points||0}/></section></>}
