import { notFound } from "next/navigation"; import { requireRole } from "@/lib/auth/require-role"; import { ContentEditor } from "@/components/admin/content-editor";
const supported=["eventos","lanzamientos","sets","historia","epk","recompensas","campanas"];
export default async function NewContent({params}:{params:Promise<{module:string}>}){const{module}=await params;if(!supported.includes(module))notFound();const{supabase}=await requireRole(["editor","admin"]);const{data:assets}=await supabase.from("media_assets").select("id,display_name,mime_type").eq("bucket","public-media").is("archived_at",null).order("display_name");return <><header className="admin-header"><div><span className="section-kicker">NUEVO REGISTRO</span><h1>Crear contenido</h1></div></header><ContentEditor module={module} assets={assets||[]}/></>}
