import{MusicLibrary}from"@/components/music-library";import{PageHero}from"@/components/page-hero";import{contentRepository}from"@/lib/data";export const metadata={title:"Música y sets"};export default async function MusicPage(){const items=await contentRepository.getSets();return <><PageHero kicker="SETS & MIXES" title="Dale play a la noche." description="Sets oficiales, contexto, energía y tracklists. Nunca reproducimos automáticamente."/><section className="section"><MusicLibrary items={items}/></section></>}
