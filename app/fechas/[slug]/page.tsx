import{notFound}from"next/navigation";import Link from"next/link";import Image from"next/image";import type{Metadata}from"next";import{contentRepository}from"@/lib/data";import{Countdown}from"@/components/countdown";import{EventActions}from"@/components/event-actions";
export async function generateStaticParams(){return(await contentRepository.getEvents()).map(({slug})=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const event=await contentRepository.getEventBySlug((await params).slug);if(!event)return{title:"Evento"};return{title:event.name,description:event.description,alternates:{canonical:`/fechas/${event.slug}`},openGraph:{title:event.name,description:event.description,type:"website",images:event.flyerUrl?[{url:event.flyerUrl,alt:`Arte de ${event.name}`}]:undefined}}}
export default async function EventDetail({params}:{params:Promise<{slug:string}>}){const event=await contentRepository.getEventBySlug((await params).slug);if(!event)notFound();const jsonLd={"@context":"https://schema.org","@type":"MusicEvent",name:event.name,startDate:event.date,eventStatus:schemaStatus(event.status),location:{"@type":"Place",name:event.venue,address:{"@type":"PostalAddress",streetAddress:event.address,addressLocality:event.city,addressCountry:event.country}},description:event.description,image:event.flyerUrl?[event.flyerUrl]:undefined,offers:event.ticketUrl?{"@type":"Offer",url:event.ticketUrl}:undefined};return <article className="event-detail-page"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd).replace(/</g,"\\u003c")}}/><div className="event-detail"><div className="event-poster">{event.flyerUrl&&<Image src={event.flyerUrl} alt={`Arte ${event.demo?"editorial de demostración":"oficial"} de ${event.name}`} fill priority sizes="(max-width:720px) 100vw, 45vw"/>}<span className="demo-badge">{event.demo?"EVENTO DEMO · EDITABLE":event.universe.toUpperCase()}</span><strong>{event.name}</strong></div><div className="event-info"><span className="demo-chip">{event.status}</span><p className="section-kicker">{event.universe.toUpperCase()} PRESENTS</p><h1>{event.name}</h1><p className="event-lead">{event.description}</p><Countdown date={event.date}/><dl><div><dt>Fecha</dt><dd>{new Date(event.date).toLocaleDateString("es-MX",{weekday:"long",day:"numeric",month:"long",year:"numeric",timeZone:"America/Mexico_City"})}</dd></div><div><dt>Venue</dt><dd>{event.venue}<br/>{event.address}</dd></div><div><dt>Horarios</dt><dd>Puertas {event.doors} · Set {event.setTime}</dd></div><div><dt>Lineup</dt><dd>{event.lineup.join(" · ")||"Por anunciar"}</dd></div><div><dt>Acceso</dt><dd>{event.age} · {event.priceLabel}</dd></div>{event.promoCode&&<div><dt>Código</dt><dd>{event.promoCode}</dd></div>}</dl><EventActions event={event}/>{event.faq?.length&&<section className="event-faq"><h2>Preguntas frecuentes</h2>{event.faq.map(item=><details key={item.question}><summary>{item.question}</summary><p>{item.answer}</p></details>)}</section>}{event.afterContent&&<section className="after-content"><span>DESPUÉS DEL EVENTO</span><h2>{event.afterContent.title}</h2><p>{event.afterContent.body}</p></section>}<Link className="text-link" href="/fechas">← Volver a fechas</Link></div></div></article>}
function schemaStatus(status:string){return status==="Cancelado"?"https://schema.org/EventCancelled":status==="Reprogramado"?"https://schema.org/EventRescheduled":status==="Finalizado"?"https://schema.org/EventCompleted":"https://schema.org/EventScheduled"}
