import{HomeContent}from"@/components/universe-content";import{contentRepository}from"@/lib/data";import{isSupabaseConfigured}from"@/lib/env";
export const metadata={alternates:{canonical:"/"}};export default async function HomePage(){const[events,sets,releases,artists,sections]=await Promise.all([contentRepository.getEvents(),contentRepository.getSets(),contentRepository.getReleases(),contentRepository.getArtists(),contentRepository.getPageSections()]);const structured={"@context":"https://schema.org","@type":"Person",name:"IAMJOSHWA",alternateName:"AFTERLUV",jobTitle:"DJ & Producer",homeLocation:{"@type":"City",name:"Ciudad de México"},genre:artists.flatMap(x=>x.genres)};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(structured).replace(/</g,"\\u003c")}}/>{!isSupabaseConfigured&&<div className="demo-notice">VISTA EDITORIAL · Fechas, lanzamientos y sets marcados como demo se reemplazarán con información oficial.</div>}<HomeContent events={events} sets={sets} releases={releases} artists={artists} sections={sections}/></>}
