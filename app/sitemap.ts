import type { MetadataRoute } from "next"; import { contentRepository } from "@/lib/data";
export default async function sitemap(): Promise<MetadataRoute.Sitemap> { const base = process.env.NEXT_PUBLIC_SITE_URL || "http://localhost:3000"; const paths = ["", "/fechas", "/musica", "/lanzamientos", "/booking", "/epk", "/media", "/historia", "/comunidad"]; const events = await contentRepository.getEvents(); return [...paths.map(url => ({ url: `${base}${url}`, changeFrequency: "weekly" as const })), ...events.map(e => ({ url: `${base}/fechas/${e.slug}`, changeFrequency: "daily" as const }))]; }
