import { requireRole } from "@/lib/auth/require-role"; import { CheckinForm } from "@/components/checkin-form";
export default async function CheckinPage({searchParams}:{searchParams:Promise<{token?:string}>}){await requireRole(["fan","editor","admin"]);const{token}=await searchParams;return <section className="auth-page"><div className="auth-card"><span className="section-kicker">CHECK-IN</span><h1>Confirma que estás aquí.</h1><p>El código se valida una sola vez desde el servidor. Nunca puedes otorgarte puntos desde el navegador.</p><CheckinForm initialToken={token||""}/></div></section>}
