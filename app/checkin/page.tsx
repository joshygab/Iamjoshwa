import { requireRole } from "@/lib/auth/require-role"; import { CheckinForm } from "@/components/checkin-form";
export default async function CheckinPage({searchParams}:{searchParams:Promise<{token?:string}>}){await requireRole(["fan","editor","admin"]);const{token}=await searchParams;return <section className="auth-page checkin-page"><div className="auth-card checkin-card"><span className="section-kicker">IAMJOSHWA PASS CHECK-IN</span><h1>Activa tu asistencia.</h1><p>Escribe el código del evento para registrar tu presencia, ganar puntos y desbloquear badges. Todo se valida desde el servidor una sola vez.</p><CheckinForm initialToken={token||""}/></div></section>}
