import{describe,expect,it}from"vitest";import{normalizeAdminValues}from"@/lib/validation/admin-content";
describe("admin content validation",()=>{it("normalizes a valid event and rejects unsafe URLs",()=>{const valid=normalizeAdminValues("eventos",{project:"iamjoshwa",name:"Night Signal",slug:"night-signal",starts_at:"2027-01-01T20:00",city:"CDMX",country:"México",publication_status:"draft",lineup:"A, B",genres:"House, Disco"});expect(valid.success).toBe(true);const unsafe=normalizeAdminValues("eventos",{project:"iamjoshwa",name:"Night Signal",slug:"night-signal",starts_at:"2027-01-01T20:00",city:"CDMX",country:"México",publication_status:"draft",ticket_url:"javascript:alert(1)"});expect(unsafe.success).toBe(false)});it("requires valid JSON in structured CMS fields",()=>{const result=normalizeAdminValues("recompensas",{project:"afterluv",name:"Private set",slug:"private-set",points_cost:"100",publication_status:"draft",requirements:"not-json"});expect(result.success).toBe(false)});it("never accepts an unknown publication state",()=>{const result=normalizeAdminValues("sets",{project:"iamjoshwa",title:"Club set",slug:"club-set",publication_status:"root"});expect(result.success).toBe(false)})});
describe("admin relational content",()=>{it("validates release platform links",()=>{const result=normalizeAdminValues("lanzamientos",{project:"iamjoshwa",name:"Signal",slug:"signal",release_type:"Single",releases_at:"2027-02-01T20:00",publication_status:"draft",release_links:JSON.stringify([{platform:"Spotify",url:"https://open.spotify.com/example",position:0}])});expect(result.success).toBe(true)});it("rejects unsafe tracklist shapes",()=>{const result=normalizeAdminValues("sets",{project:"afterluv",title:"Hard session",slug:"hard-session",publication_status:"draft",set_tracks:JSON.stringify([{position:1,title:""}])});expect(result.success).toBe(false)})});
